import classes from "./MainNavigation.module.css";

const Header = () => {
  return <header className={classes.module}>
      <p className={classes.logo}>Great Quotes</p>
  </header>;
};

export default Header;
